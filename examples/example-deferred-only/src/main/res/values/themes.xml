<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="Base.Theme.DetourAndroidSDK" parent="Theme.Material3.DayNight.NoActionBar">
         <item name="colorPrimary">@color/primary</item>
    </style>

    <style name="Theme.DetourAndroidSDK" parent="Base.Theme.DetourAndroidSDK" />

    <!-- Splash screen theme: shown while getDeferredLink() resolves -->
    <style name="Theme.DetourAndroidSDK.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/primary</item>
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_launcher</item>
        <item name="postSplashScreenTheme">@style/Theme.DetourAndroidSDK</item>
    </style>
</resources>
